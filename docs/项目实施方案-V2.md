# 项目实施方案 V2

V2 是当前正式方案，已经落地到 `src/` 目录。它在 V1 的基础上进一步规范了代码结构、训练入口、推理入口和提交流程，并给出后续提分实验顺序。

## 当前 V2 代码对应关系

当前方案和代码的匹配关系如下：

- 路径与训练超参数配置：`src/config.py`
- 数据扫描、数据审计、训练/验证/测试变换：`src/dataset.py`
- ConvNeXt Tiny 模型定义：`src/model.py`
- 5-fold 训练、Weighted CE、best 指标选择、warmup + cosine、AMP：`src/train.py`
- 多 fold 推理、hflip TTA、多尺度 TTA、生成 `result.txt`：`src/infer.py`
- 提交文件格式校验：`src/validate_submission.py`

## V2 当前默认配置

V2 当前默认配置与本方案保持一致：

- `NUM_CLASSES = 40`
- `FOLDS = 5`
- `MODEL_NAME = "convnext_tiny"`
- `IMG_SIZE = 256`
- `BATCH_SIZE = 16`
- `EPOCHS = 25`
- `LR = 3e-4`
- `WEIGHT_DECAY = 1e-4`
- `WARMUP_EPOCHS = 3`
- `USE_WEIGHTED_CE = True`
- `SAVE_METRIC = "blend"`
- `AMP = True`

## 第 0 步：先明确输入输出

### 已知数据

- 训练集目录：`data/train/0` 到 `data/train/39`
- 测试集目录：`data/test/`
- 测试集文件名清单：`data/testpath.txt`
- 类别映射：`data/garbage_dict.json`

### 最终输出

生成 `result.txt`，要求：

- 恰好 400 行
- 每行格式：`图片名 标签`
- 标签必须是 `0..39`
- 顺序必须严格按照 `data/testpath.txt`

## 第 1 步：先做数据审计

这一步不要训练，先确认数据没问题。

### 1.1 检查训练集目录

确认：

- `data/train/0..39` 共 40 个文件夹都存在
- 每个文件夹里都有图片
- 图片扩展名统一可读，比如 `.jpg`、`.png`、`.jpeg`

### 1.2 检查测试集

确认：

- `data/testpath.txt` 恰好 400 行
- 每一行图片名在 `data/test/` 中都存在
- 之后推理时，只认 `testpath.txt`，不要直接遍历 `data/test/`

### 1.3 检查标签映射

看一下 `data/garbage_dict.json`：

- key 是否是 `"0"` 到 `"39"`
- value 是否是中文类别名
- 训练和提交时都只用数字标签，中文名称主要用于调试和分析

## 第 2 步：构建训练样本索引

不要依赖旧的 `train.txt` / `val.txt` 形式，直接从目录扫描。

### 2.1 扫描规则

遍历：

- `data/train/0/*.jpg`
- `data/train/1/*.jpg`
- ...
- `data/train/39/*.jpg`

每张图记录两项：

- `image_path`
- `label`

例如：

```text
data/train/12/img_001.jpg -> 12
```

### 2.2 标签使用原则

直接使用文件夹名作为标签：

- 文件夹 `0` 就是标签 `0`
- 文件夹 `39` 就是标签 `39`

不要重新编码成别的顺序，否则很容易提交错标签。

## 第 3 步：做 5-fold 分层划分

这是正式方案的核心之一。

### 3.1 为什么用 StratifiedKFold

因为类别大概率不均衡，分层划分可以让每一折的类别比例更稳定。

### 3.2 推荐设置

- `n_splits = 5`
- `shuffle = True`
- `random_state = 42`

### 3.3 每一折得到什么

每一折会得到：

- `train_idx`
- `val_idx`

然后训练一个独立模型：

- `fold0`
- `fold1`
- `fold2`
- `fold3`
- `fold4`

最终推理时对 5 个 fold 的输出做平均。

## 第 4 步：定义图像预处理和增强

垃圾分类场景背景乱、角度乱、光照乱，所以增强很重要，但不要太重。

### 4.1 输入尺寸

V2 当前主配置：

- `img_size = 256`

后续提分实验可尝试：

- `img_size = 320`

### 4.2 训练增强

V2 当前使用：

- `RandomResizedCrop(img_size, scale=(0.8, 1.0))`
- `RandomHorizontalFlip(p=0.5)`
- `RandomRotation(15)`
- `ColorJitter(brightness=0.2, contrast=0.2, saturation=0.2, hue=0.05)`
- `ToTensor()`
- `Normalize(mean=ImageNet, std=ImageNet)`
- `RandomErasing(p=0.25)`

### 4.3 验证 / 测试增强

V2 当前实现使用：

- `Resize(round(img_size * 1.14))`
- `CenterCrop(img_size)`
- `ToTensor()`
- `Normalize(mean=ImageNet, std=ImageNet)`

后续提分实验可重点对照：

- `Resize(int(img_size * 1.14)) + CenterCrop(img_size)`
- 多尺度推理输入，例如 `256 / 288 / 320`

## 第 5 步：定义模型

### 5.1 主模型

首选：

- `ConvNeXt Tiny`
- `pretrained=True`
- `num_classes=40`

### 5.2 为什么它适合

- 比传统 ResNet 更强
- 比大 transformer 更稳
- 对自然图像分类迁移效果好
- 对细粒度差异有不错表现

### 5.3 预训练来源

直接用：

- `ImageNet-1K` 预训练权重

## 第 6 步：处理类别不均衡

### 6.1 先统计每类样本数

遍历训练集后，得到每个类别的样本数量。

### 6.2 推荐做法

V2 当前使用：

- `Weighted CrossEntropy`

权重采用温和的 `sqrt inverse frequency` 思路，避免少数类权重过大。

### 6.3 对照实验

后续可以做：

- `Weighted CE = True`
- `Weighted CE = False`

对照，看是否真的带来收益。

## 第 7 步：训练配置

### 7.1 优化器

- `AdamW`

### 7.2 学习率

- `lr = 3e-4`

### 7.3 weight decay

- `1e-4`

### 7.4 学习率调度器

- `warmup + cosine`

### 7.5 epoch 数

- `epochs = 25`

后续实验可试：

- `30~35`

### 7.6 batch size

V2 当前：

- `batch_size = 16`

后续实验可试：

- `24`
- `32`

### 7.7 混合精度

- `AMP = True`

## 第 8 步：训练循环怎么做

### 8.1 每轮训练记录这些指标

- `train_loss`
- `val_loss`
- `val_acc`
- `macro_f1`

### 8.2 保存最优模型

每个 fold 保存一个最佳 checkpoint，例如：

- `outputs/checkpoints/convnext_tiny_fold0_best.pth`
- `outputs/checkpoints/convnext_tiny_fold1_best.pth`
- ...
- `outputs/checkpoints/convnext_tiny_fold4_best.pth`

### 8.3 保存标准

V2 当前实现默认按：

- `0.5 * val_acc + 0.5 * macro_f1` 的综合分数最大

后续优化实验建议重点对照：

- `val_acc` 最大
- `macro_f1` 最大

## 第 9 步：做 5-fold 正式训练

V2 当前主线就是：

- `fold0 ~ fold4`
- 每折 `20~25 epochs`
- 输入尺寸 `256`
- 使用加权 CE
- 保存每折 best model

## 第 10 步：测试阶段推理

### 10.1 推理输入来源

严格读取：

- `data/testpath.txt`

不要直接遍历 `data/test/`。

### 10.2 每张图的推理过程

对于 `testpath.txt` 中每个文件名：

1. 读入 `data/test/<image_name>`
2. 做测试变换
3. 分别送入 5 个 fold 模型
4. 得到 5 份 logits 或 probability
5. 做平均
6. argmax 得到最终类别

## 第 11 步：加轻量 TTA

V2 当前使用：

- 原图
- 水平翻转图
- 可选多尺度：`--tta-sizes 256,288,320`

### 11.1 当前做法

对每个 fold、每张图：

- 预测原图
- 预测水平翻转图
- 两者概率平均

然后再对 5 个 fold 平均。

### 11.2 后续提分方向

- 多尺度 TTA：`256 / 288 / 320`
- 与第二模型融合后的集成推理

## 第 12 步：生成 result.txt

每行写：

```text
image_name<TAB>label_id
```

例如：

```text
test1.jpg<TAB>29
```

注意事项：

- 无表头
- 一行一个结果
- 中间一个 tab 字符
- 标签必须是整数 `0..39`
- 行数必须正好 400
- 顺序必须和 `data/testpath.txt` 完全一致

## 第 13 步：提交前强校验

提交前一定做这几项检查：

- `result.txt` 恰好 400 行
- 文件名顺序和 `data/testpath.txt` 完全一致
- 所有标签都在 `0..39`
- 没有空行、没有多余空格、没有末尾异常格式

## 第 14 步：V2 提分实验顺序

在 V2 主线已经稳定、平台分数约 91% 的前提下，实验路径应明确按三梯队推进，而不是把所有增强项一次性混入。

需要特别说明的是：当前代码优先落地第一梯队，也就是 `评估变换优化`、`best checkpoint 选择标准优化` 和 `多尺度 TTA`。第二梯队和第三梯队仍按下面顺序逐步引入，避免同时改动过多导致无法判断收益来源。

### 第一梯队：立刻做

1. **E1：评估变换优化**
   - 比较 `Resize((256,256))` 与 `Resize(292) + CenterCrop(256)`
   - 目标：先修正验证/测试分布与训练分布的差异
2. **E2：best checkpoint 选择标准优化**
   - 对照 `val_acc`、`macro_f1`、`0.5 * val_acc + 0.5 * macro_f1`
   - 目标：找到更适合不均衡类别任务的 best 保存标准
3. **E3：多尺度 TTA**
   - 在 hflip 基础上增加 `256 / 288 / 320` 多尺度推理
   - 目标：用低风险方式继续抬高推理上限

### 第二梯队：筛出后再做

1. **E4：batch size / warmup 联动**
   - 例如对照 `batch_size=16/24/32` 与 `warmup=3/5`
   - 目标：提高训练稳定性与收敛质量
2. **E5：img_size 提升到 320**
   - 在显存允许前提下提高输入分辨率
   - 目标：增强细粒度类别识别能力
3. **E6：weighted CE 与 plain CE 对照**
   - 对照 `Weighted CE = True/False`
   - 目标：验证类别重加权是否持续带来收益
4. **E7：label smoothing**
   - 建议从 `0.05` 起步
   - 目标：改善过拟合与边界样本鲁棒性

### 第三梯队：冲分项

1. **E8：第二模型 EfficientNetV2-S**
   - 训练与 ConvNeXt Tiny 互补的第二模型
   - 目标：准备后续融合
2. **E9：双模型融合**
   - 推荐起步：`0.6 * ConvNeXt + 0.4 * EfficientNet`
   - 目标：利用不同模型错误模式互补继续提分
3. **E10：高置信伪标签**
   - 只引入高置信测试样本进行再训练
   - 目标：利用测试分布进一步冲榜

### 执行原则

- 第一梯队优先级最高，应先完成并记录收益。
- 第二梯队只在第一梯队结果稳定后再推进。
- 第三梯队属于冲分项，适合在主模型方案已经基本收敛后再做。
- 每次实验尽量只改变一个关键变量，避免多个变量同时变化导致结论失真。

## 最终推荐参数表

上面的三梯队实验顺序优先于参数堆叠。也就是说，先按 `E1 -> E2 -> E3` 完成第一轮高价值实验，再考虑把第二梯队和第三梯队的改动逐步合入最终参数表。

### 主模型

- `ConvNeXt Tiny`
- `ImageNet-1K pretrained`
- `num_classes = 40`

### 训练

- `img_size = 256`
- `optimizer = AdamW`
- `lr = 3e-4`
- `weight_decay = 1e-4`
- `scheduler = cosine + warmup`
- `epochs = 20~25`
- `loss = Weighted CrossEntropy`
- `AMP = True`

### 数据

- `StratifiedKFold(n_splits=5, shuffle=True, random_state=42)`

### 增强

- `RandomResizedCrop`
- `HorizontalFlip`
- `RandomRotation`
- `ColorJitter`
- `RandomErasing`

### 推理

- 5-fold 平均
- 水平翻转 TTA
- 按 `data/testpath.txt` 顺序输出

## 一句话落地版

V2 的核心是：基于 `src/` 目录的正式实现，使用 ConvNeXt Tiny 的 ImageNet 预训练权重完成 5-fold + Weighted CE + 256 输入 + hflip TTA 的稳定主线，再通过验证变换、best checkpoint 标准、多尺度 TTA、第二模型融合和伪标签逐步提分。

V1 方案见 [docs/项目实施方案-V1.md](项目实施方案-V1.md)
